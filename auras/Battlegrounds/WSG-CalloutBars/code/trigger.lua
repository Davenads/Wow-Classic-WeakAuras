-- WSG Callout Bars — Trigger 1 → Custom → Status (Check On: Event)
-- Every icon is always shown while loaded (zone = Warsong Gulch). The real behavior is
-- the click button built in init.lua; this trigger just keeps the region visible.
function()
    return true
end

